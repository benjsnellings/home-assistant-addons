#!/command/with-contenv bash
# Do NOT run full Claude/Cursor/Pi updates here — npm/curl downloads can hang and
# Supervisor will kill the add-on mid-boot. Image already ships working CLIs;
# the tool-updater s6 service refreshes them in the background after start.
exit 0
