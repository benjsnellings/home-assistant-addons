
# Access-mode defaults for this add-on variant (appended by sync-addons.sh)
ENV \
    MULTICA_ACCESS_MODE=rw \
    MULTICA_DEFAULT_DEVICE_NAME="HA Config (read-write)" \
    MULTICA_DEFAULT_RUNTIME_NAME=""
