name: Multica Daemon (read-only)
version: "0.2.0"
slug: multica_ro
description: >-
  Multica agent daemon with a read-only Home Assistant config mount. Use this
  for safer agents that can inspect config and query states/history but cannot
  modify files.
url: https://github.com/benjsnellings/home-assistant-addons/tree/main/multica_ro
arch:
  - aarch64
  - amd64
startup: services
boot: auto
hassio_api: true
hassio_role: default
homeassistant_api: true
map:
  - type: homeassistant_config
    read_only: true
    path: /config
  - type: share
    read_only: false
  - type: ssl
    read_only: true
  - type: media
    read_only: false
options:
  multica_token: ""
  server_url: "https://api.multica.ai"
  app_url: "https://multica.ai"
  workspace_id: ""
  device_name: ""
  runtime_name: ""
  anthropic_api_key: ""
  max_concurrent_tasks: 2
  log_level: info
schema:
  multica_token: password?
  server_url: url
  app_url: url
  workspace_id: str?
  device_name: str?
  runtime_name: str?
  anthropic_api_key: password?
  max_concurrent_tasks: int(1,20)
  log_level: list(trace|debug|info|warning|error)
